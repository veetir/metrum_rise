# Metrum Rise — Roadmap

Active tracked work uses stable IDs instead of positional numbering. This file is the live planning surface for priorities, validated bugs, and later tracks.

Status values:
- `open`
- `in_progress`
- `needs_revalidation`
- `done`
- `parked`

Priority values:
- `P0`
- `P1`
- `P2`

Kind values:
- `feature`
- `bug`
- `hardening`
- `refactor`
- `docs`

## Active Priorities

`AUDIT-01` — `hardening`, `parked`, `P1`. Paused by user at a validated checkpoint; resume only when requested. Persistent state is in `audit-state/` at the project root. Audit the existing codebase in subsystem passes, fix confirmed obsolete/duplicate code and correctness/scaling findings, and distinguish source inspection from fresh correctness and performance validation. Coverage and acceptance evidence: [`code_audit.md`](code_audit.md). Exit: every source area has a documented pass, confirmed findings are resolved or explicitly tracked, and affected behavior is validated.

| ID         | Kind        | Status        | Priority | Owner doc                                        | Problem                                                                                                                                                                                   | Exit criteria                                                                                                              |
| ------------| -------------| ---------------| ----------| --------------------------------------------------| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------| ----------------------------------------------------------------------------------------------------------------------------|
| `CIV-01`   | `feature`   | `done` | `P1`     | [`economy.md`](economy.md)                       | First city-owned service placement is live and gameplay-validated: Services toolbar discovery, road-frontage placement, treasury build cost, city-funded wages, per-service utility fee routing, coal-fueled power input purchases from the city treasury, aggregate power production/payment settlement, Rust-owned daily budget overview ledgers, a live electricity funding policy slider that controls default staffed power-worker slots, and per-plant funding overrides. A 39-day economy run validated coal power placement, coal freight, funding-sensitive production, local utility revenue, OWA fallback on under-coverage, and recovery to full power coverage after increasing funding. | Player can place at least one utility building that verifiably changes household and commercial/industrial operating cost and stability. |
| `WATER-01` | `refactor`  | `in_progress` | `P1`     | [`terrain.md`](terrain.md)                       | The legacy dynamic water prototype has been removed; baseline still water remains. Water mesh topology now builds through async Rust/Rayon cache jobs with Godot submitting time-capped request batches and polling Rust-owned ready meshes under pending-job backpressure; uploads apply under measured time and byte budgets, stale road/depth signatures are rejected before upload, stale queued water-mesh requests are compacted before submission, request/cache/job perf counters expose queue health, fully wet unclipped patches use indexed grid buffers plus shared Godot `ArrayMesh` reuse by LOD/topology/size with regular full-grid variants prewarmed during load, terrain shoreline sampling reuses the Water renderer's resident depth texture instead of uploading a duplicate terrain-aligned water texture, Rust now returns texture-ready height/depth byte payloads for patch image uploads, regular terrain mesh variants are prewarmed from the active layout, terrain/water patch nodes/materials/images/textures are pooled before first visible activation, perf summaries include viewport/draw/primitive/memory/vsync/FPS-cap render stats plus resource-pool counters, gameplay world-load refresh now consumes render-dirty flags so the first live frame does not repeat resident terrain/water uploads, terrain/water residency plus cache prewarm now use elapsed-time budgets instead of fixed patch counts, water steady-state residency follows terrain's resident-set revision, terrain/water mesh-LOD refreshes plus terrain-to-water texture sync are time-budgeted queue drains instead of full resident sweeps, activation/streaming drains remove far patches first while pushing camera-near texture/LOD/mesh work first, water mesh ready polling adaptively fills a bounded camera-sorted apply queue with a conservative high-backlog headroom boost, terrain/water LOD refreshes are movement-gated with per-frame check/change caps plus stale pending sweep replacement and target-diff queue filtering, speculative prewarm is bounded to the near-resident halo, baked/CDT terrain patches skip no-op LOD mesh rebuilds, and water mesh refresh now polls ready work, applies ready uploads, then submits new work last under a soft scheduler budget. Large authored-map validation still needs fresh perf captures. | Baseline water rendering no longer depends on full resident-patch refreshes, shoreline/coast rendering remains stable, and any future flowing-water design starts as a new bounded system rather than reviving the removed prototype. |
| `ZONE-01`  | `refactor`  | `done` | `P1`     | [`zoning.md`](zoning.md)                         | Rust-owned `20 m x 20 m` default road-aligned parcels are the normal-runtime zoning authority. Godot submits input and uploads Rust-produced parcel overlay geometry. | Allocator and demand consume parcel legality, parcel occupancy is saved and restored through stable parcel IDs, and pre-zoned or free parcels can be created, extended, and rezoned without a zoning paint surface. Drag-run creation keeps legal candidates instead of cancelling the whole stroke at the first blocked parcel. |
| `ZONE-03` | `feature` | `done` | `P1` | [`zoning.md`](zoning.md) | Hover a road segment to preview parcels on both sides with the selected profile, frontage, depth and gap; press once to create currently valid lots. | Rust owns picking, deterministic layout and all legality checks. Manual drags and automatic fill share a layout anchored to existing parcels, respecting mixed lot widths and the selected gap. Slopes, bends and endpoint rounding retain the shared spacing corrections. Legal endpoint attachments round inward without cancelling both rows or weakening saved frontage bounds. Every density uses the same terrain check for the selected lot dimensions, independent of assets. Missing compatible assets permit terrain-valid zoning while growth waits for suitable content. Existing parcels remain unchanged, blocked lots are skipped, road previews cache by edge/options/dependencies, actual site failures use red previews without cursor warnings, and off-road parcel gestures remain available. |
| `TERRAIN-01` | `refactor` | `done` | `P1` | [`terrain.md`](terrain.md) | Terrain and water used to depend on one whole-map dense upload and one whole-world mesh boundary, making denser authored terrain and local engineered-ground visuals pay a full-world render cost that originally only made sense for simple overlay-style grids. | Terrain rendering uses chunk-local patch upload and residency instead of whole-map dense refreshes, water rendering follows the same bounded patch grid, the old whole-map Godot render APIs are removed from the steady render path, and the `10 m` versus `5 m` terrain decision can now be re-measured on the split path. |
| `TERRAIN-02` | `bug` | `done` | `P1` | [`terrain.md`](terrain.md) | Runtime grass albedo/height imports disabled mipmaps despite mipmapped shader sampling; the source-image fallback generated them, but normal imports loaded only the 2048x2048 base. | Both imports generate full chains and the shared runtime loader verifies 11 mip levels. All 48 GPU-profiled and 48 unprofiled road fixtures pass. Same-session RX 7900 XTX diagnostics reduce median periodic GPU time from 2.978 to 0.638 ms; this is not an uncapped FPS or frame-spike fix. |
| `EARTH-02` | `refactor` | `in_progress` | `P1` | [`earthworks.md`](earthworks.md) / [`asset_editor.md`](asset_editor.md) / [`building_allocator.md`](building_allocator.md) | Buildings and usable yards share a level pad with 2 m graded perimeter strips. Zoned, service and explicit industry placement share support preparation, installation and terrain invalidation; 2D/3D frontage drift and the explicit raw-height fallback are removed. Hillside residential placement now uses feasible-asset selection, a constrained pad-height solve, terrain-neutral zoning feasibility/reasons and local dependency caching. Paired zoned/service regressions compare final site and terrain buffers; existing Kuopio, coverage, picking and RoadEditPlan tests remain. The `farms.sqlite` terrain rejection is reproduced and fixed through shared guide heights and building-apron boundary grading (CDT revision 14). The unsaved live view remains unavailable; renderer publication and construction animation remain distinct from final geometry checks. | Building structures and usable yards remain level; edge tie-ins meet road, neighbor and terrain boundaries without missing or overlapping covers. Placement, local rebuilds, RoadEditPlan adoption, removal, save/load, queries, and populated-map performance remain deterministic and verified. |
| `ROAD-01`  | `refactor`  | `done` | `P1`     | [`roads.md`](roads.md)         | Surface roads used a centerline-first render/terrain model with separate preview, mesh, terrain-flattening, and water-validity solves. On sloped authored worlds this produced floating or buried road edges and left no single authoritative roadbed surface. The roadbed rewrite now splits graph topology from the visible carrier, routes `Span`, `Bend`, `Terminal`, and `JunctionN` through owner-local band carriers, preserves canonical carrier provenance, validates production authored plus imported DEM terrain agreement without compatibility repair, and rejects `Standard` roadbed footprints over authored water while leaving explicit bridges legal. | One deterministic roadbed model owns preview, committed surface mesh, lane-marking anchors, world-surface queries, water-crossing validity, and chunk-local stitched terrain. The visible carrier remains `Span`, `Bend`, `Terminal`, and `JunctionN`, with arbitrary-angle `2..N` node ownership, profile-preserving degree-two pass-through handoffs with one shared endpoint cross-section, symmetric structural bridge-to-ground ramps that never stamp terrain fill, source-owned grounded-abutment cutouts that exclude elevated bridge runs, final road-owned terrain footprints, source-provenanced terrain-CDT seams whose patch selection and source queries share one padded boundary, widened tie-in diagnostics, structural retaining-wall tie-ins, and no source-vector repair, boundary snapping, flattening elevated junctions, or compatibility fallbacks. |
| `ROAD-02`  | `hardening` | `done` | `P2`     | [`roads.md`](roads.md)         | Generated road-surface helper coverage now varies mixed sidewalk / curb / shoulder profile modes across the existing mixed-width node matrix. | Generated flat and elevated node-helper coverage exercises mixed sidewalk / curb / shoulder profile-mode variation while preserving canonical identity, provenance, and terrain-CDT gates. |
| `ROAD-03`  | `hardening` | `done` | `P1`     | [`roads.md`](roads.md)         | Grounded `Standard` road terrain tie-ins could still show jagged CDT teeth or ordinary-road retaining-wall output beside the road-owned footprint on steep authored / imported terrain. | Road-touched terrain now inserts explicit Rust-generated grade-limited guide samples around grounded `Standard` footprints, ordinary standard/node seam sources never emit retaining-wall topology, and structural bridge / tunnel retaining paths remain source-provenanced. |
| `ROAD-04`  | `hardening` | `done` | `P1`     | [`roads.md`](roads.md)         | Elevated or curved `Bend` / `JunctionN` top surfaces can produce visually harsh asphalt bumps, acute triangles, or over-steep platform planes even when terrain ownership, render upload, and road-footprint clipping are correct. Debug logs showed road-owned top-surface triangles with near-zero edges, extreme aspect / slope values, and steep but otherwise planar JunctionN outputs. | Node top-surface triangulation canonicalizes same-owner / same-height / same-provenance numeric-dust support, inserts deterministic road-owned interior guide vertices before CDT when a sloped carriageway region has verified grade-plane support, regrades affected junction road-edit mouths through a bounded horizontal-distance platform solve that selects the best stable opposite-mouth authority corridor for the whole `JunctionN`, prevents secondary opposite branch pairs from rotating that base plane, blends non-authority edited branches with a small dynamic mouth pin, stable solve/control sample, sparse outer transition supports, and protected handoff sampling, rejects visible pathological carriageway triangles with source-rich diagnostics, preserves final footprint / earthwork provenance, and uses no fallback sampling, render bias, or hidden terrain repair. |
| `ROAD-05`  | `refactor` | `in_progress` | `P0` | [`roads.md`](roads.md) / [`terrain.md`](terrain.md) | Fixed world-aligned bounded CDT tiles, immutable previous-generation reuse, tile-local fingerprints, local dirty ledgers, Rayon builds, cached per-tile render buffers, contributor-only Spade planning with regular-filler ownership of empty neighbors, single-pass contributor grading margins, terrain-span-sized committed-road render chunks with accumulated delta/tombstone publication, deterministic generation/upload scaling benchmarks, bounded incremental road undo with exact pre-edit surface-cache restoration, stable exact-XZ `JunctionN` contact reuse, unchanged same-material and cross-kind raised-step pair reuse, indexed contributor-local raised-step passes, final noded-component replay, reverse-indexed retained-contact authority/decision reuse, node-local topology identity, exact contributor-local canonical ownership cleanup/self-touch/seam/final-boundary point reuse, exact final-boundary assembly replay, indexed boundary construction, uniform-height whole-ownership reuse, semantic node-export reuse, atomic exact-generation publication, generation/input-checked reuse of exact async road-preview validation, required-neighborhood-only preview validation compilation, bulk-finalization-identical preview profile scope, and exact preview-to-commit junction rail/ownership/attached-arrangement replay are implemented. The incremental captures exposed stale contributor-manifest and margin-only ownership bugs, reused-patch assembly cost, an undo publication stall, remote third-road splits that rejected both affected `JunctionN` pieces, and a replacement-world generation stall that suppressed authored water. These now have targeted regressions; failed required-node generations retain the last complete surface/mesh, replacement worlds publish their final road generation before payload workers resume, water-only query revisions retain unchanged road clipping, and fresh gameplay profiles pass. Complete assembled node-export buffers remain. | Refined terrain uses fixed world-aligned bounded CDT tiles with exact contributor/grading coverage and full tile-local fingerprints; unchanged tiles reuse immutable geometry and render buffers from the last accepted generation; changed work is `old_coverage union new_coverage` plus deterministic seam dependencies, including bounded undo; contributor-free dirty neighbors return to side-manifest-aware regular filler instead of entering Spade; removed coverage restores regular terrain; changed tiles build through Rayon; committed road rendering rebuilds and uploads only the affected surface/earthwork chunk union while preserving unique triangle ownership and exact-generation terrain-before-road publication, with fixed-workload Rust/Godot benchmarks separating resident-size scaling from changed-payload cost; bounded undo restores the affected pre-edit surface records and rebuilds only old-plus-restored chunk shells; stable JunctionN contact topology, unchanged exact contour/source/cross-kind pair contributors, final noded components, relevant retained decisions, uniformly translated whole ownership, exact unchanged cleanup/self-touch/seam/final-boundary contributors, exact matching final footprint/seam/arrangement assemblies, spatially indexed final explicit-step topology, exact-XZ height-conflict cohorts, top-boundary contributors, raised-step spans/faces, and exact preview-produced attached arrangements bypass repeat work with deterministic cold fallback; current-generation conflict aggregation, cancellation, deduplication, orientation, sorting, and positional-index binding remain authoritative; fixed source/target tiles make later raised-step passes visit only new sources, changed target groups, and their spatially overlapping pairs; retained authority queries and fingerprints use exact reverse-indexed buckets, while a changed authority generation still traverses current source geometry once; raw graph edge-ID churn does not invalidate node-local topology; exact async preview reuse requires generation, prepared points, lane counts, snap mode, canonical node-local rail topology, carrier registry, and exact carrier heights to match; preview validation compiles only the required local incidence closure while retaining exact required-piece verification; exact preview profile solving must use the same bulk split-edge dirty ledger as commit and reuse every eligible double-T span/node artifact; remote edge splits compile every affected old/new required node before publishing; runtime replacement and water-only generation paths preserve coherent terrain/water clipping; targeted reuse, change, removal, seam, manifest, ownership, undo, topology, failure-atomicity, generation, and validation-certificate tests plus deterministic road-render scaling benchmarks pass; release gameplay replay confirms junction compiler replay and leaves road-triggered terrain generation/render as the dominant end-to-end boundary. |
| `ROAD-08`  | `bug` | `done` | `P0` | [`roads.md`](roads.md) / [`terrain.md`](terrain.md) | The controlled matrix's mild 90 m curved Kuopio approach at `(-1920, -1280)` appeared to leave the exact compiled preview pending for more than 90 seconds. Instrumentation proved the compiler had already returned `surface_geometry_invalid` in `26–37 ms`; RoadTool retained the earlier cheap valid verdict, and the harness therefore waited for an impossible valid exact result. | Exact results replace cheap verdicts even when invalid and the harness terminates on completed invalid previews. The original fix reported rejection in `41–75 ms`. Current geometry accepts and commits this curve: schema 3 removes the stale rejection expectation and verifies settlement plus a degree-two junction at the unchanged site; two clean-world replays pass. |
| `DEM-01`   | `hardening` | `done`        | `P1`     | [`demand.md`](demand.md)                         | Immigration admission and displacement ownership still leaks through allocator and transport leftovers.                                                                                   | Demand-layer outputs fully own immigration and displacement decisions with old cross-system leftovers removed.             |
| `ALLOC-01` | `hardening` | `open`        | `P1`     | [`building_allocator.md`](building_allocator.md) | The building allocator still has known spec and ownership limitations before it can become the long-term growth executor.                                                                 | Documented allocator limitations are either removed or explicitly bounded in the owning spec and code.                     |
| `RENDER-01` | `feature` | `in_progress` | `P2` | [`terrain.md`](terrain.md) | The world had one fixed sun angle and one fixed palette, so the simulation's operational clock had no visible consequence and every screenshot read as the same hour. A day/night cycle is now live: `day_cycle.gd` maps the day fraction to a solar position and an elevation-keyed palette, a single key light carries the sun by day and an antisolar moon at night, and terrain/water/site ground follow through shader globals. New games start at `07:30` rather than midnight. Still open: no seasonal declination sweep, no street or window lighting at night, and the laptop cost is unmeasured. | Lighting is a pure function of the simulation clock, the whole day sweeps without a visible discontinuity, no key light shines up through the ground, night stays readable without street lighting, and the cycle costs no measurable GPU frame time against the fixed-sun baseline. |
| `RENDER-02` | `bug` | `open` | `P2` | [`terrain.md`](terrain.md) | Vegetation level-of-detail is decided once per terrain patch, because Godot tests one distance for a whole `MultiMeshInstance3D` and one instance carries a whole `510 m` patch. A plant therefore gets the level its patch centre asks for, with up to `361 m` of error, and half a square kilometre of vegetation switches as a unit. Three symptoms follow. Understory appears as a curtain when a patch crosses `BUSH_RANGE_M`, and near trees and every vegetation shadow appear together at `TREE_NEAR_M`, so one zoom step can cross both. Branched near crowns are drawn out to `800 m`, where their detail is smaller than a pixel and aliases. And `TREE_NEAR_M` cannot be shortened to fix that, because a band narrower than the patch diagonal (`721 m`) would let a patch draw distant crowns for plants `39 m` from the camera. A crossfade is not the answer: `VISIBILITY_RANGE_FADE_SELF` alpha-blends the whole instance and drops it from the depth pre-pass, which cost 17-28% of frame time and was removed. The aerial seam at `TREE_NEAR_M` exchanges four things at once: scissored foliage coverage for a solid shell, palette-gated backlight for a plain material, moving branches/cards for static crowns, and a branch skeleton for a lathe. Mid/far also collapse all variants to zero, losing pine silhouettes. The earlier description incorrectly treated this as only a mesh-authoring gap. A 2026-09-13 headless CPU reference raster at equal apparent height measures near→mid coverage steps of +52.18/+52.06 percentage points (conifer/broadleaf); opaque-card controls account for most of that, while backlight is small under the fixed test light and conifer width still changes by 8.82 pixels. A shared distant scissor/backlight material first reduced those coverage steps to +4.52/-1.05 points. The filtered-hash follow-up suppresses and darkens distant wood, uses derivative-selected blended octaves and measures +3.65/+1.73 near→mid and -3.17/-1.05 mid→far points at 16 px, without changing ranges, near meshes, surfaces or primitives; RGB-vector near→mid error decreases for both species, with individual colour-channel regressions. The raster then found the larger defect underneath: the near crown filled only `.1417`/`.1527` of its own silhouette against `.5533`/`.6560` for the same meshes with opaque cards, so the baked foliage mask, not the geometry, was removing three quarters of the canopy, and near fill fell with distance because a card's cluster vanished outright once a mip held one texel per cell. With the mask fitted to its cell, densified and made to fill rather than vanish, near fill becomes `.4162`/`.5003` and stops falling. Those coverage figures were then found to be measured through a blind spot: `coverage` is fill of a crown's own bounding box and the raster scales every level to one apparent height, so both of a crown's size terms divide out and two levels can agree on coverage while one hides half the ground the other does. A `footprint` metric in world metres now records beside it, and it showed the distant lathe was built at the branch base rather than at the foliage extent, at `4.70 m` against a `9.68 m` near conifer crown. Sizing the distant crowns from the measured near foliage envelope moves footprint against the near crown from `52.8`/`47.1` to `101.7`/`93.0` percent for conifer and from `47.2`/`57.6` to `103.9`/`88.4` for broadleaf, adding no triangle at any level. Paired `E13` GPU captures on a GTX 1060 3GB price the change at `-0.098` to `+0.224 ms` of scatter cost across five views, three of them negative, below the `0.25 ms` within-trial spread, with one extra draw call on two views and identical video memory; on screen the distant canopy darkens the ground it stands on by `42%` more. Colour, wind, variant and patch-granularity discontinuities remain. See [the measurement tables and limits](terrain.md#vegetation-lod-reference-measurements--render-02-2026-09-13). A 2026-09-16 change takes the colour half. The key light runs its cascades to `SHADOW_MAX_DISTANCE_M` (`420 m`), so every crown past that distance is lit on each surface the sun can reach and a stand loses the shadows its own members cast on each other; that loss begins at the cascade edge, not at the mesh change at `TREE_NEAR_M`. All three vegetation shaders now multiply albedo and backlight by one per-instance canopy shade term that fades in exactly where the cascades fade out, so nothing inside shadow range changes. The term is smooth value noise over `48 m` cells keyed on the instance origin, which the near and the distant mesh of one tree share, so a tree keeps its shade across the level switch. Its mean is `0.70`, the share a canopy keeps when about two fifths of its visible surface sits in shadow at ambient level. A paired synthetic overhead capture on a GTX 1060 3GB measures the nearest rows unchanged to four decimal places and the mid and far canopy `10-19%` darker; `METRUM_CANOPY_SHADE` scales the term, with `0` removing it. Appearance in a real world is unverified, and the patch-granularity defect above is untouched. A 2026-09-16 probe then found a second defect under the patch-granularity one, and it removes trees rather than shading them. Godot measures a visibility range from the instance bounds and not from the node origin: a real-renderer probe put two `MultiMeshInstance3D` at the camera's own position and culled the one whose single instance sat past the range. Each near level holds one variant and the distant level holds the whole species, so their bounds centres differ. On a random scatter of `396` stems in `12` variants that offset is `12 m` on average, `24 m` at the 90th percentile and `67 m` at worst. The `(0, TREE_NEAR_M)` and `(TREE_NEAR_M, TREE_FAR_M)` ranges are complementary by construction and were not complementary in practice: across the metres between the two centres the distant level had already stopped and the near level had not yet started, and the trees of that variant were drawn by neither. About half the variants sit on the losing side of that, which thins the canopy as the camera closes on `TREE_NEAR_M` and fills it back in a moment later. A paired probe on a GTX 1060 3GB reproduced it: both levels drew `0` pixels at a camera position between the two centres, and one level drew `72` pixels again after both were given the union of their bounds. Every level of a patch now takes that union as its `custom_aabb`, computed once per upload and never per frame. The headless dummy renderer reports empty multimesh bounds, so the regression holds the shared-bounds contract and not a measured box. The reference raster prices what the switch still exchanges: at `16-24 px` the distant conifer crown covers `107.1%` of the near crown footprint and the distant broadleaf `105.5%`, so the two representations differ by under `8%` once both are actually drawn. Patch granularity, the understory curtain at `BUSH_RANGE_M` and on-screen appearance are untouched. A 2026-09-16 capture over a brush-painted dense stand (`imgs/reference/game/16-09-lod-blocky*.png`) shows the patch boundary as a staircase of `510 m` squares between a dark closed canopy and a pale field, and that capture rules the crown representation out as the cause of the pale. Three fresh measurements: the distant crown colour is the area-weighted mean of the near mesh, and restricting that mean to foliage moves it by `4.1%` for both species, because wood is only `8.7%`/`3.1%` of near crown area; the mean lit RGB per covered pixel at `20 px` agrees between the near crown and the distant crown within `10%`; and the near-crown footprint the distant crown covers rises with camera elevation, from `107.1%`/`105.5%` at `45 degrees` to `116.4%`/`128.1%` at `65` and `124.6%`/`152.4%` at `80`. The distant crown is therefore brighter and larger than the near crown from a high camera, which is the wrong sign for a pale far field, so the missing dark mass is the understory that is built only inside `UNDERSTORY_PATCH_RANGE_M` and drawn only to `BUSH_RANGE_M`. The raster now takes an `--elevation` argument, because the two representations agree at one elevation and not at another. Two changes follow, both deterministic and neither costing a frame. Bush and rock carry a single level, so a variant that stops early drops nothing into a gap; each of the twelve variants now ends at its own share of the range, spread by a low-discrepancy sequence over the last quarter of it, so the carpet dissolves over `105 m` instead of ending on one line. The shared-bounds merge above is also narrowed from the patch to one species, because the complementary pair is a species' own near and distant levels and a bush must not measure its `420 m` range from a box that holds a `25 m` canopy. And each patch now swaps its canopy on its own distance, `TREE_NEAR_M` less up to `CANOPY_SWITCH_JITTER_M` (`160 m`) from a hash of the patch key, so the switch circle is no longer one circle. The offset is negative only, which leaves `_near_band_wanted` the conservative superset it already was and draws the near level over less ground, never more. The colour step itself is reduced, not removed, and is unverified on screen; the remaining suspects are the understory mass and the ground shadow that ends with the cascades at `SHADOW_MAX_DISTANCE_M`, neither of which has a paired in-engine measurement yet. A 2026-09-19 capture (`imgs/reference/game/19-09-lod-issue.png`) names the second suspect and it is confirmed. `RENDER-05` publishes forest-floor land cover at unchanged albedo luminance, on the rule that only real tree shadows may darken ground. That rule holds inside the cascades and fails outside them: past `SHADOW_MAX_DISTANCE_M` no tree shadow exists, so the floor of a closed stand returns to open-meadow brightness while the crowns standing on it keep the canopy shade term added on 2026-09-16. The far field therefore reads as lit meadow carrying dark specks where the near side of the same stand reads as closed forest, which is the colour step the screenshots show. The terrain shader now takes the receiver half of the same term: one ramp over the same `327.6-420 m` fade, scaling albedo toward `CANOPY_FLOOR_SHADE_FLOOR` in proportion to the crown coverage the shader already fetches. That floor is derived and not chosen, as `(GROUND_SHADOW_AMBIENT + GROUND_SHADOW_SUN_STRENGTH * GROUND_SHADOW_MIN_VISIBILITY) / (GROUND_SHADOW_AMBIENT + GROUND_SHADOW_SUN_STRENGTH)`, or `0.51`, which is what ground the cascades did darken keeps where sun and sky are of comparable strength. Coverage is the union of accepted crown discs, so a lone tree on a meadow darkens almost nothing, a generated stand at `30.5` stems/ha reaches about `8%` coverage and a brush-painted stand at `625` stems/ha reaches about `83%`. The cost is one smoothstep, one mix and one multiply on a fetch the shader already made, and the ramp is radial about the camera, so it adds no patch edge. `METRUM_CANOPY_SHADE` now scales both halves of the term, which moved to `SceneLightingConfig.canopy_shade_strength`. A rendered `gl_compatibility` regression measures the contract on the GTX 1060: ground with no coverage is identical at `10 m` and at `600 m`; covered ground at `10 m` is identical with the term at `0` and at `1`; and covered ground at `600 m` measures `0.3203` against `0.6480`, a ratio of `0.494` against the `0.51` linear floor, the difference being tone mapping. Appearance in a real world is unverified, and patch granularity is untouched. A 2026-09-19 capture after that change (`imgs/reference/game/19-09-lod-after-fix.png`) still shows the step, and a first in-engine measurement then found why a week of work did not move it. Every colour figure above comes from `tools/vegetation_lod_measure.py`, a CPU raster under a fixed test light with no engine ambient, no engine backlight and no tone map. It reported the two levels agreeing on lit RGB within `10%`. A probe that puts both levels on the shipped GPU with the shipped shaders over one population of `625` stems/ha measures the distant level at `1.28x` the near level's crown luminance for conifer and `1.36x` for broadleaf, and that ratio holds at `380 m`, `900 m` and `1800 m`. Distance is not the variable: the same near level changes by `8%` across that whole range, so neither the cascade edge nor the canopy shade term is the cause. The same probe measures crown ground coverage at `0.92-0.98` for all three levels at that density, so coverage is not the cause either. The cause is that the levels were fitted on albedo when the invariant is rendered radiance: the near crown is a cloud of cards whose normals point everywhere, which shadow each other and hide a dark interior, while the distant crown is one convex lathe whose every visible face turns outward and takes the key light almost fully, so one mean albedo on the lathe draws far brighter than the crowns it stands in for. `DISTANT_RADIANCE_MATCH` now scales distant albedo and backlight per species. The values are fitted and not derived, because the tone map makes rendered luminance sublinear in albedo: a sweep puts them at `0.558` and `0.505`, which bring the ratio to `1.037` and `1.006`. `vegetation_level_match_test` holds this on a real display and fails at `1.322`/`1.360` with the term removed. The CPU raster keeps its use for silhouette and footprint work and is no longer evidence about colour. A 2026-09-19 change takes the third of the four things the level switch exchanges, movement. Sway is authored in metres and drawn in pixels and nothing connected the two: the term is bounded by `0.50 m` and applies unchanged to `TREE_NEAR_M`, where one pixel covers `1.14 m` of world at `1080p` and the engine's default `75 degree` field of view, so the whole motion is under half a pixel. Motion that small cannot move a crown's outline; it can only flip alpha-scissor pixels along every cutout edge in the stand, which is what a stand seen from altitude reads as a boil rather than as wind. A GPU probe over `625` stems/ha prices it: with no gate, `42%` of the stand's crown pixels change by more than `0.02` luminance between a still and a displaced stand at `1500 m`, where the sway measures `0.18 px`, against `57%` at `150 m` where it measures `1.83 px`. Both shaders that displace now scale the offset by a screen-space gate, `smoothstep` over `0.6` to `2.0` pixels of that `0.50 m`, which at `1080p` and `75 degrees` is full sway inside `176 m` and none past `586 m`. Measured after the change: `55%` at `150 m`, `13%` at `300 m` and `0%` from `500 m` out, so close-up liveliness is unchanged and the boil is gone. `PROJECTION_MATRIX` reads as a zero matrix in the vertex stage, which silently removed the wind from the whole game on the first attempt and which neither shader compilation nor any existing test caught, so the one projection term comes from `CAMERA_FOV_DEG` as a uniform while `VIEWPORT_SIZE`, which is populated, supplies the rest and a window resize needs no update. A shadow-pass check confirms a tree and its shadow still displace together. `vegetation_wind_gate_test` holds both ends on a real display and fails with the gate forced to `1.0` and with it forced to `0.0`. Wind exists only on the near level, so this also quiets the `TREE_NEAR_M` boundary, where movement previously stopped dead. Appearance in a real world is unverified, and patch granularity is untouched. | Vegetation fades in per plant rather than per patch, using alpha hashing so the material stays in the opaque pass with depth write intact; `TREE_NEAR_M` is short enough that branched crowns are not drawn past the distance they were authored for; and no single camera step makes a visible block of vegetation or shadow appear at once. An intermediate crown exists between the branched near mesh and the smooth mid cone, so no camera step exchanges branch geometry for a cone in one jump, and the level count is set by measured silhouette error against primitive count rather than by the three levels that happen to exist. |
| `RENDER-03` | `feature` | `open` | `P3` | [`terrain.md`](terrain.md) | Vegetation and terrain art is judged from memory, so a change can drift a long way from the Finnish boreal landscape the world is modelled on before anyone notices. The 2026-09-12 session found the ground under forest kept an open-meadow colour, the canopy sat `20-50x` below real stem density, and no birch existed at all, none of which was visible from the code. `imgs/` already holds a few captures and one reference photograph, but it is excluded through `.git/info/exclude` and nothing in it is tracked, so adopting this means deciding both where the set lives and whether third-party photographs can be redistributed under the repository's licence; captures the project renders itself carry no such question. | A small committed set of reference photographs covering the boreal cases the renderer has to hit (aerial closed canopy, lake shore, forest interior, open field, winter) sits beside matched in-game captures from comparable cameras, so a person or an agent can put the two side by side and judge whether a change moved toward the reference or away from it. |
| `RENDER-04` | `feature` | `open` | `P2` | [`terrain.md`](terrain.md) | Canopy scatter is about `30 trees/ha` against `500-1500/ha` in a real Finnish stand, and the `16 m` candidate grid caps it at `39/ha` however the acceptance probability is tuned, so a closed canopy is arithmetically unreachable. Raising it is blocked by the far band, not the near one: quadrupling canopy density measured `+25.4 ms` and `34.2 M` primitives on `yaw180` against a `+5.59 ms` baseline, with draw calls unchanged, because `TREE_FAR_M` reaches `4500 m` and the `2000-4500 m` ring alone covers `51 km2`. The same experiment on the understory, which reaches only `420 m`, cost `+1.89 ms` for the same factor and shipped. | Canopy density reaches a closed-canopy read at ground level and from the air without the far band's primitive count following it, through a distance-graded population, a cheaper far representation such as octahedral impostors, or both; measured against matched unprofiled `E13` sweeps. |
| `RENDER-05` | `bug` | `done` | `P2` | [`terrain.md`](terrain.md) | The previous ground mask was fully removed: the terrain shader had no land-cover input, leaving bright meadow grass under closed canopy. The old row incorrectly described a shipped water/slope-gated GLSL mirror. Forest-floor substrate must follow the accepted generated and authored canopy, including tombstones and indexed water/road/site/relief rejection, while canopy shadows remain the only tree light occlusion. | Rust publishes a fixed 8 m R8 coverage texture from accepted crown discs, including neighboring contributors and a bilinear filter border. Terrain stages/reuses its textures and checks the existing terrain and vegetation revisions in the contributing patch neighborhood. The shader blends litter/moss/duff at unchanged albedo luminance with one coverage fetch. Complete texels are compared against accepted canopy products across edits, surface rejection and a patch boundary; native upload/reuse and both invalidation streams have a headless regression. On-screen appearance remains unverified. |
| `RENDER-06` | `bug` | `open` | `P2` | [`terrain.md`](terrain.md) | Species do not read as the species they name. Every tree mesh is procedural GDScript in `tree_species.gd`, built once at startup with `SurfaceTool`; no tree is a Blender asset, so this is a change to profile and branch arithmetic and not to an art pipeline. Revised 2026-09-19 against the shipped build: the pine gap this entry opened with is closed. `_is_pine` makes two of every three conifer variants pine, `_pine_trunk` carries the long bare trunk, `PINE_CROWN_BARK` the orange-red upper bark, and `_crown_reach` holds a separate curve per silhouette, so manty and kuusi are now distinct forms. Four items remain. First, spruce does not land: the drooping-bough sign is there (`0.12` for pine against `-0.14` for spruce) but the crown reads as a sparse dark spike rather than the dense narrow spire of a Finnish kuusi, which carries foliage nearly to the ground. This is crown density and lower-crown reach, not silhouette. Second, the player cannot choose a species: `main_ui.gd` offers `Conifer`, `Broadleaf`, `Bush` and `Rock`, and the pine/spruce and birch/aspen choice is made by the generator from position, so a player who wants a spruce stand cannot plant one. Carrying a variant would have to reach the addition delta, and only the near level pays for variants, because mid and far share variant zero's mesh for the whole species; a player-placed spruce therefore reverts to the species form past `TREE_NEAR_M` until that is addressed too. Third, birch bark is too weak and too even: `_birch_trunk` places `8` bands at a uniform `0.10 * height` spacing from `7%` to `77%` of height, each `35-75 mm` tall, and the single dark ring spans `y = -0.4` to `0.35 m`, most of it below the ground, so a mature trunk shows no dark base. Fourth, the third broadleaf variant in each group of three (`_is_birch` is `variant % 3 != 2`) is a generic broadleaf with no identity, and aspen is the Finnish occupant of that slot. Measuring the meshes also found a scale disagreement that is not fixed here: a near crown spans about `4.7-5.9 m` of half width, while `land_cover.rs` splats the forest floor from crown radii of `3.0 m` and `3.5 m`, and a mature Finnish crown is nearer the second figure than the first. The drawn crowns are therefore roughly `1.7x` the radius the simulation believes they have, which widens every tree against the `30 trees/ha` of `RENDER-04` and is a candidate cause of the sparse, oversized read from the air. That radius is separable from the species work and is the piece worth pulling forward. | Spruce reads as a Finnish kuusi from the ground without being named, and the brush can place a named species rather than a species group. Birch shows a dark base on a mature trunk, and the third broadleaf variant is an aspen. Each silhouette is traceable to a reference photograph under `imgs/reference` rather than to memory. Startup mesh construction and the per-patch draw-call count do not increase, and the `land_cover.rs` crown radii still bound every crown the renderer draws.
| `VEG-01` | `feature` | `done` | `P1` | [`terrain.md`](terrain.md) | **Must decide and must have before any upstream PR.** Vegetation is not world state. `get_decorative_tree_patch` is a pure function of position, documented as cosmetic only, persisting nothing; no save schema mentions it, no tree has an identity, and the player cannot plant, clear or touch a single one. The terrain shader then paints a forest-floor substrate from `woodland_field`, a hand-maintained GLSL mirror of the same sine field, so the ground is coloured as forest whether or not one tree stands on it - disabling the vegetation renderer leaves the mask fully intact, and a per-fragment mirror of a procedural field can never do otherwise. Two questions are open and neither has an answer in the code: whether a world ships pre-populated or is planted by the player, and whether placement is per-tree, brush, or an auto-populate pass the player can re-run. Scale rules out storing a population outright: Finnish stand density over the default world is around `10^10` stems. Both questions are now decided: a world ships empty or generated, chosen when the game is started rather than baked into the `WorldDefinition`, so one authored world serves both; and placement is a brush that accepts a one-tree radius, with clearing the same brush. The generator takes `enabled`, `seed`, stand coverage and in-stand stems per hectare, and all four are saved state rather than video settings, because an addressable tree is gameplay state and must be identical on every machine. Coverage and in-stand density are separate axes with different costs: coverage moves the `woodland()` acceptance threshold and is free, while stems per hectare is set by `CANOPY_CELL_M` and is bounded by `RENDER-04` - 16 m at 0.78 acceptance is 30 trees/ha, and a real 500-1500 trees/ha needs a 2.6-4.5 m cell, 13-38x the instances. Coverage control also needs a field the current one cannot provide: `woodland()`'s wavelengths are fixed in world metres at 2027 m and 785 m, so across the 500 m sandbox world the field never completes a period and the whole map falls inside one stand. | Ground land-cover is separated from canopy occlusion and each comes from its real source: occlusion from the shadows of the trees that are actually there, land cover from the accepted population rather than from a field the shader re-derives. A tree is addressable state the player can add and remove, stored as a sparse authored delta over the deterministic generator - tombstones and additions, sized by player edits and not by stem count - persisted through the save schema, invalidated by an independent vegetation generation for the render patch that carries it, and covered by a test that adds and removes vegetation across a patch boundary and a save round trip — **done**, save format 61, with the tool, the ground-ring cursor and the independent patch revision all in place. The land-cover half now uses accepted generated and authored canopy crown coverage (`RENDER-05`), with both revision streams and headless edit/boundary regressions — **done**; on-screen appearance remains unverified. The generator is parameterised by a saved seed, coverage and density, reads them from the save rather than from constants, and produces a stand pattern that still varies across a 500 m world - **done**, save format version 60. Those parameters are chosen when a world is selected for a new game, from a dialog whose defaults and accepted density range are read from Rust rather than copied into GDScript, so the renderer's density ceiling stays a single number - **done**. The stand field remains a generator decision and is never mirrored in GLSL. |
| `VEG-02` | `bug` | `open` | `P3` | [`terrain.md`](terrain.md) | The bulldoze tool deletes one building or road per click and reports its target through `get_bulldoze_target_at`, which answers only `building` and `road`. A plant is addressable state that the vegetation tool can clear, so bulldoze silently ignores a class of object the player can place, and the only way to remove a tree is to open the Terrain submenu. `remove_vegetation_at` already removes a single plant at the minimum radius, so the gap is the target query and the hover preview, not the mutator. | `get_bulldoze_target_at` answers a vegetation target when no building or road covers the cursor, with a hover shape the player can see, and `bulldoze_at` removes that one plant through the existing edit store. The vegetation tool keeps its own clear for area work, because bulldoze is one target per click. A test places a plant, bulldozes it and checks the same tombstone the tool's clear would write. |
| `VEG-03` | `feature` | `open` | `P3` | [`terrain.md`](terrain.md) | The generator grows one forest everywhere. `evaluate_cell` chooses conifer or broadleaf from the same field over the whole world, with saved coverage and density as the only controls, so the wet-to-dry site gradient that decides which species grow in a Finnish forest has no representation. Cases the world cannot express: spruce-dominated moist heath, pine-dominated dry esker and rocky outcrop, birch-dominated fresh heath, and open or near-open peat bog. Most inputs exist already, because `placement_clear` reads water, relief and the indexed surfaces and the watermap holds baseline depth, so a per-cell moisture and relief read needs no new grid. Northern stunting belongs to the world rather than to the cell: a 20 km world lies inside one region, so a Lapland world is a saved world-level term on height and diversity, not a field sampled per plant. This is blocked on `RENDER-06`, because a biome that selects between species is worth nothing while the species all look alike. | Species mix and stem size follow a per-cell site read covering at least wet, fresh and dry sites, plus a world-level northern term that lowers height and narrows the species set. The read uses the existing grids and indices and adds no third spatial structure. `scatter_layer` stays a pure function of position and seed, so no population is stored. Wet sites are visibly open rather than forested. |
| `VEG-04` | `feature` | `open` | `P3` | [`terrain.md`](terrain.md) | Rowan (pihlaja) is missing, and it is the tree that says 'Finnish street' rather than 'Finnish forest'. It is a garden and verge tree in built-up areas, not a stand species, so it cannot ride the `evaluate_cell` canopy field that `VEG-03` refines - that field answers what grows on a site, and rowan is planted rather than grown. It is also seasonal: a rowan reads as a rowan through its orange berry clusters, which are only present from about August to October, and the world has no season term at all, only the `RENDER-01` day clock. Its form is small and multi-stemmed, around `6-10 m`, well under the `10.3-22.0 m` every broadleaf variant currently spans, so it does not fit the `1.3x` height band the appearance test holds a species inside and needs its own species ID rather than a broadleaf variant. Adding a fifth species ID costs a `VARIANT_COUNTS` entry, a `CROWN_RADII_M` entry and one more `MultiMesh` bucket per patch, which is a draw call per patch per level. | Rowan exists as its own species, placed along streets and around buildings rather than scattered through stands, at a density low enough that it reads as planted. Its berries appear on a seasonal term rather than year round. The per-patch draw-call count does not rise for a patch that contains no rowan. |
| `VEG-05` | `bug` | `done` | `P2` | [`terrain.md`](terrain.md) | The paint brush is as sparse as the generator, because it *is* the generator. `paint_at` walks the canopy grid, offers exactly the one candidate `candidate()` places in each cell, and keeps it only where `evaluate_cell` rejected a plant. At the shipped 16 m canopy cell that is one stem per 256 m2, a ceiling of 39 stems/ha, and inside a stand at `CANOPY_STAND_ACCEPT` `0.78` only 22% of cells are free, so a stroke over existing forest adds about 8.6 stems/ha and reads as doing nothing. The player also gets the generator jitter rather than the coverage they painted, so a stroke cannot produce a deliberately dense planting, a hedge, a row, or a rock field. The brush is bounded by its own disc rather than by world area, so it is not what `MIN_CANOPY_CELL_M` and `RENDER-04` exist to protect: that ceiling caps generated density over the whole world, and a stroke is paid for once where the player drew it. | A stroke places plants on its own lattice at a spacing independent of the canopy cell, dense enough to reach the 400-700 stems/ha of a real Finnish stand, and fills every point its footprint clears rather than every point the generator would have accepted. Placement stays deterministic and stays a pure function of position and seed. The edit store keeps addressing plants by canopy cell, so the tombstone and restore contract of `VEG-01` is unchanged and restoring an individual generated tree as its original species still collapses to no stored edit; a dense disc retains its additional lattice plants. Shipped with a 4 m lattice (625 points/ha), at most 0.2 m jitter per axis, a 256 m planting-radius limit and at most 20,866 planning slots. A 64 m release stroke adds 862 plants in 709.83 us versus 50 in 112.24 us before. Rust vegetation/full suites and all four headless bridge regressions pass; see the dated VEG-05 section for commands and limits. |
| `DOC-01`   | `docs`      | `in_progress` | `P1`     | [`README.md`](README.md)                         | The docs restructure is not finished and some live references still point back to old numbered backlog habits.                                                                            | `project.md` stays dashboard-sized, roadmap IDs remain stable, and remaining numbered live references are retired.         |

## Code Quality / Technical Debt

| ID        | Kind       | Status | Priority | Owner doc                                        | Problem                                                                                                                                                                                                                                                                                                                                         | Exit criteria                                                                                                                                                                                                                                                                                  |
| -----------| ------------| --------| ----------| --------------------------------------------------| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `CODE-01` | `refactor` | `open` | `P0`     | [`entrance_and_exit.md`](entrance_and_exit.md)   | `rust/src/simulation/economy/agents/tick.rs` is a monolithic "god loop" that forces trip planning, local-access geometry, unsafe SoA movement, congestion, and cache maintenance into one massive file.                                                                                                                                         | Agent trip planning, movement, and congestion/cache responsibilities live in smaller focused modules (e.g., `planning.rs`, `movement.rs`, `congestion.rs`) with current behavior and test coverage preserved.                                                                                  |
| `CODE-02` | `refactor` | `open` | `P2`     | [`project.md`](project.md)                       | `godot/scripts/asset_editor.gd` and `economy_editor.gd` act as "god classes" that manually construct complex UIs via code and pack layout, state, I/O, and bridge logic into oversized scripts.                                                                                                                                                 | Editor shells are refactored into a strict Model-View-Controller pattern, pushing all UI assembly into dedicated `*_view.gd` scripts.                                                                                                                                                          |
| `CODE-03` | `refactor` | `open` | `P2`     | [`economy.md`](economy.md)                       | `rust/src/simulation/economy/definitions.rs` combines authored-economy schema, validation, sandbox simulation, and export/index IO in one module.                                                                                                                                                                                               | Authored economy schema, validation, sandbox execution, and export/index generation are separated into focused modules with the same external behavior.                                                                                                                                        |
| `CODE-04` | `refactor` | `open` | `P2`     | [`building_allocator.md`](building_allocator.md) | `rust/src/simulation/buildings/allocator/lifecycle.rs` has a deeply nested 180-line monolith (`cleanup_stale_buildings`) mixing footprint/legality checks with allocator mutation.                                                                                                                                                              | The compatibility checks are extracted into a clean helper method (e.g. `is_building_still_compatible`) to flatten the loop and improve maintainability.                                                                                                                                       |
| `CODE-06` | `refactor` | `open` | `P2`     | [`project.md`](project.md)                       | `rust/src/nodes/sim/render/network.rs` relies on repetitive ~45 line boilerplate for `dict.set(...)` packed array buffer uploads.                                                                                                                                                                                                               | The verbose dictionary uploads are replaced with a concise declarative macro or mapping function.                                                                                                                                                                                              |
| `CODE-07` | `refactor` | `done` | `P0`     | [`economy.md`](economy.md)                       | The old monolithic `demand.rs` and `households.rs` mixed runtime orchestration, data storage, viability, snapshots, action planning, settlement, employment, replenishment, relocation, and tests in oversized files. | `demand.rs` and `households.rs` now act as API routers around focused sibling modules, with demand planning/snapshot/action logic and household tick/data/employment/replenishment/relocation/removal logic split into bounded files while preserving behavior and library test coverage.                                                                                                     |
| `CODE-08` | `refactor` | `in_progress` | `P2`     | [`project.md`](project.md)                       | The original `SimulationNode` monolith is now split into lifecycle/routing, focused Godot API modules, async job state, and Variant export adapters, while `nodes/sim/core` owns authoritative state, threading, snapshots, and several pure build paths. Godot-independent refined-terrain/CDT input, cache, regular-grid geometry, and output-analysis work still lives under `nodes/simulation_node`. | Move the remaining Godot-independent terrain/CDT preparation, caching, geometry, and output analysis into simulation/core-owned modules; keep `SimulationNode` responsible for locking/dispatch and translating prepared Rust results into Godot Variants and packed arrays. |
| `CODE-09` | `refactor` | `open` | `P2`     | [`project.md`](project.md)                       | Pure-code UI instantiation (e.g. `Label.new()`, `VBoxContainer.new()`) has leaked across basic tool-scripts like `main_ui.gd`, `road_tool.gd`, and `move_tool.gd`.                                                                                                                                                                              | Systematically sweep `.gd` scripts and extract all UI creation boilerplate into dedicated programmatic `*_view.gd` layouts.                                                                                                                                                                    |
| `CODE-10` | `refactor` | `open` | `P1`     | [`project.md`](project.md)                       | The `.unwrap()` plague: over 26 core simulation files rely on blind unwraps, causing unrecoverable panics instead of deterministic fallbacks.                                                                                                                                                                                                   | Systematically replace `.unwrap()` calls in the `rust/src/simulation` directory with safe `if let`, `match`, or strictly defined deterministic fallbacks.                                                                                                                                      |
| `CODE-11` | `refactor` | `open` | `P2`     | [`entrance_and_exit.md`](entrance_and_exit.md)   | `rust/src/simulation/economy/agents/tick.rs` uses `thread_local! { RefCell<Vec<_>> }` scratch buffers. This is a safe and sound pattern (each Rayon worker thread owns its own instance), but it introduces implicit per-thread global state that is invisible to callers and harder to test.                                                   | Replace the thread-local scratch buffers with explicit `&mut Vec<_>` parameters threaded down through the hot-path call stack.                                                                                                                                                                 |
| `CODE-12` | `refactor` | `in_progress` | `P2`     | [`terrain.md`](terrain.md)                       | The `9 km` camera/far fade hides forward horizon activation, but the resident and prewarm regions still follow the current view frustum. A first-time abrupt yaw reversal can therefore expose an unprepared region, and the former two-patch-per-frame activation drain made that handoff last seconds. | Ready terrain/water activation now bursts to `12` patches under `4 ms` budgets; a Forward+ 180-degree probe replaced a `196`-patch view in `13` frames instead of the old theoretical minimum of `98`. Full closure still requires a bounded yaw-independent ready region or an explicit rotation transition, without reverting to whole-world residency. |
| `CODE-13` | `refactor` | `done` | `P2`     | [`terrain.md`](terrain.md)                       | Terrain and water non-mesh patch payload preparation used to sit on the Godot hot path, and the first refined-terrain async path still built road/site CDT inputs while holding the central simulation mutex. A building-spawn capture measured a `16.7 s` mutex hold that stopped simulation, input handling, and logging together. | Terrain/water residency, speculative prewarm, and resident dirty uploads request async Rust-prepared payloads and poll them later. Refined terrain workers hold `SimCore` only for bounded patch-local snapshots, perform indexed clipping/grading/CDT work off-lock, use patch-local source revisions, coalesce to one physical build per patch/render-step, and discard stale results. Godot still owns GPU resource upload on the main thread. |
| `CODE-14` | `refactor` | `done` | `P1`     | [`roads.md`](roads.md)         | Road-surface internals now use `glam`-backed `RoadVec2` / `RoadVec3` for long-lived numeric geometry across span, node, terrain-clip, earthwork, query, and cache products. Godot `Vector2` / `Vector3` values remain only at graph/API input, render upload, debug output, and GDExtension adapter boundaries.                                          | Road-surface internals use `RoadVec2` / `RoadVec3` for numeric geometry, canonical keys / stable IDs remain the only topology identity, and Godot vectors are limited to graph/API input, render upload, debug output, and GDExtension boundary adapters.                                      |

## Validated Bugs

| ID       | Kind  | Status   | Priority | Owner doc                  | Problem                                                                                                                                                                                                                         | Exit criteria                                                                                                                                                         |
| ----------| -------| ----------| ----------| ----------------------------| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------| -----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `ECON-06` | `bug` | `done` | `P1` | [`economy.md`](economy.md#field-staffing-density-econ-06) | Grain farms advertised eight jobs per hectare, overloading the city employment model. | Authored staffing area gives one worker per ten hectares with a two-worker minimum; yield stays per hectare and staffing, demand, startup payroll, saved-area reconstruction, and export reserves share the density and minimum. Targeted economy regressions pass. |
| `ECON-07` | `feature` | `done` | `P1` | [`economy.md`](economy.md#field-placement-and-editing-econ-07) | Fields did not reserve land against roads, parcels or buildings, and existing polygons had no inspector editing workflow. | Bidirectional placement clearance, inspector vertex editing with atomic release/recalculation, invalid-move rollback, visible farm-lot and building/yard guides during drawing/resizing, and reservation reconstruction through load/removal/undo. |
| `ECON-08` | `feature` | `done` | `P1` | [`economy.md`](economy.md#farm-households-econ-08) | Farms need one resident household while keeping independent area-scaled employment. | One normal family slot, shared household economy, on-site adult work, commuter hiring, and housing preserved through resizing. Audited asset round trips, bankruptcy vacancies, and family identity after removal; duplicate housing paths removed. No aging or inheritance system. |
| `ECON-09` | `feature` | `done` | `P1` | [`economy.md`](economy.md#machinery-upkeep-econ-09) | Production and utilities need recurring equipment demand to support upstream industry. | Machinery inputs, paid imports, area/operation scaling, startup buffers, resource-specific industrial growth, stable resource identities and inspector stock/rates are implemented. The small factory profile imports Steel/Metals and is ready for the user's forthcoming asset. |
| `EARTH-01` | `hardening` | `done` | `P1` | [`earthworks.md`](earthworks.md) | Road-touched terrain reports widened near-seam samples, preserves exposed road constraints, uses grade-limited ordinary tie-ins for grounded `Standard` roads, and keeps explicit retaining-wall output reserved for structural / future retaining sources. Imported JunctionN internal road-owned chords are removed from the terrain seam constraint set only after both sides classify as road-owned. | Road-touched terrain seam diagnostics enforce a deterministic steepness limit across representative DEM cases without moving the road-owned seam, hiding it with strips, or emitting ordinary standard-road wall teeth. |
| `EARTH-03` | `hardening` | `done` | `P1` | [`earthworks.md`](earthworks.md) / [`roads.md`](roads.md) | Roadbed grading-envelope guide generation lived in the Godot-facing terrain-CDT input path and constrained guide rails even for concave or multi-loop junction footprints, allowing helper constraints to recreate the invalid mouth-angle failures they were meant to smooth. | `RoadSurfaceSystem` owns grading-envelope generation from the final unioned roadbed loops, inserts grade-limited guide samples before terrain source samples, constrains guide rails only for a single clean convex loop, keeps hole / concave / multi-loop sets sample-only, excludes building-site loops from roadbed grading, and validates concave junction-style footprints through sample-only guide envelopes. |
| `NET-01` | `bug` | `closed` | `P0`     | [`project.md`](project.md) | Parallel or overlapping road placement silently skips junction creation, producing topologically disconnected edge segments that CCH cannot route across. All freight between affected buildings falls back to OWA permanently. | `collect_crossing_splits` correctly detects and splits parallel/overlapping distinct edges; local freight routes successfully between buildings without OWA fallback. |
| `ROAD-09` | `bug` | `done` | `P1` | [`roads.md`](roads.md) | Near-boundary vertex recovery and integer-overlay roundoff lost terrain boundary sources; metric short-edge rejection broke closed earthwork spans. Loading also resnapped saved road grades. | Exact rectangle intersections and canonical short-edge provenance survive clipping; earthwork normals accept nonzero edges; load preserves saved road geometry. The `iso.sqlite` replay compiles all 37 roads and 192 terrain windows without boundary or constraint failures and preserves road geometry through another save/load. |
| `ZONE-02` | `bug` | `done` | `P1` | [`zoning.md`](zoning.md) | Curved-run spacing could advance beyond legal frontage after the initial station check, producing saved parcels rejected with `FrontageOutOfBounds`. | Spacing validates the final persisted attachment; saved endpoint overruns use the existing parcel/building quarantine lifecycle. Curved-run and invalid-save regressions pass. |
| `ROAD-10` | `bug` | `done` | `P1` | [`roads.md`](roads.md) / [`zoning.md`](zoning.md) | The iso road attempts hit hidden empty parcels while previews reported valid; closely spaced profile knots also folded span bands into an invalid boundary loop. | Road/walkway mode shows parcel constraints; all preview paths use commit's indexed parcel guard and invalidate on zoning revisions. Continuous span frames preserve exact node mouths. All 1,528 Rust tests and the Godot bridge pass; both logged shapes report parcels 280/281 in all three preview paths, then commit and compile all five terrain patches on a disposable copy with the four empty parcels removed. |
| `ROAD-11` | `bug` | `done` | `P1` | [`roads.md`](roads.md) | Resolving a numeric-dust gap checked the entire union contour and rejected a valid 12 cm curb step about 80 m away. The iso `(22, 1)` terrain failure blocked the new road mesh and left the patch absent after reload. | Recovery checks only the connected dust run and its source anchors through the existing source-edge index; local conflicting heights still reject. The isolated regression fails before the fix and passes with cyclic/reversed contour and source orders. All 1,530 Rust tests, both Godot bridge regressions, and the saved-map replay pass: five terrain patches and six road chunks on load, another save/load, and road removal/rebuilding. |
| `ROAD-12` | `bug` | `done` | `P0` | [`roads.md`](roads.md) / [`terrain.md`](terrain.md) | Simulation accepted roads before terrain validation; a capped complete-boundary cleanup retained a microscopic junction hole with conflicting curb heights in iso patch `(23, 1)`. | Validate and cache actual terrain products before routing/charging, roll back graph and split dependents on failure, and apply the existing boundary precision without a fixed total-area cap. All 1,535 Rust tests and both Godot bridges pass; the 52-road save produces five terrain patches/six road chunks through save/load, and the previous live-road replay passes with one Rayon worker. |
| `ROAD-13` | `bug` | `done` | `P1` | [`roads.md`](roads.md) | Deleting connector 47 then redrawing N43–N45 discarded valid preview artifacts on a frontier failure; deleted-edge duplicate detection then suppressed the replacement, while global bulldoze lane rebuilding invalidated distant agents. | Full/incremental validation retain required artifacts without relaxing live atomic publication; duplicate checks ignore tombstones; bulldozing uses the existing incremental lane closure. Three red-before/green-after regressions and all 1,555 Rust tests pass. Certified/uncached commits and one-worker replay restore 52 roads, six chunks, and five terrain patches through save/load. |
| `ROAD-14` | `bug` | `done` | `P1` | [`roads.md`](roads.md) / [`terrain.md`](terrain.md) | A local patch union received a whole 2,790 m span footprint, overflowing the micrometre i32 overlay. Terrain-query exports now use existing section-station runs and handoff provenance; full render/earthwork outlines and precision are unchanged. | The isolated regression fails before and passes after the fix. All 1,545 Rust tests pass, and the unchanged side-32 setup verifies all 1,024 grid degrees and 1,984 edges and completes both local strokes. |
| `ROAD-15` | `bug` | `done` | `P1` | [`roads.md`](roads.md) | A contended ghost-guide fetch returned an unavailable dictionary that Godot treated as an empty world, hiding guides and letting benchmarks skip their cost. | Unavailable data retains the mesh and retries once next frame; successful data carries its generation. Reverified after `ROAD-13`: bridge retry/empty-world regression and both side-0/8/16/32 captures pass with current visible guides; comparison rejects missing, stale, or mismatched output. |
| `ROAD-16` | `bug` | `done` | `P1` | [`roads.md`](roads.md) | Identical grid replays produced different ghost vertex counts: hash-map split order assigned different edge IDs, which feed profile authority; intersection candidates also inherited R-tree traversal order. | Stable local candidate/split ordering and node-ID tie breaks preserve allocated edge IDs. Reverified after `ROAD-13`: reversed batch-order regression, all 1,555 Rust tests, and two independent side-0/8/16/32 replays pass with identical guide counts and graph/lane cardinalities. |
| `ROAD-17` | `bug` | `done` | `P1` | [`entrance_and_exit.md`](entrance_and_exit.md) | Faster contraction ordering exposed incorrect route costs: shortcut construction discarded alternatives, including cheaper detours beside direct arcs, and one exhausted query frontier could terminate both searches. | Retain lower-triangle choices with exact boundary/mode state equivalence, reselect after metric changes, and terminate frontiers independently. All-pairs shortest-cost, direct-arc detour, re-customization and turn-aware Dijkstra regressions pass. |
| `ROAD-18` | `hardening` | `done` | `P2` | [`roads.md`](roads.md) | The guide/CCH pass exposed a warmed empty-network first-stroke regression (36.2 to 41.2 ms). Cheap authoring commands still waited behind the simulation thread's uninterruptible 60 Hz sleep. | Commands now interrupt the existing channel wait and publish edit snapshots without advancing movement early. Three matched warmed pairs reduce first-road latency 43.7 to 30.8 ms and queue wait 7.33 to 0.079 ms; side-32 gains and matched guide/graph/lane output are retained. All 1,557 Rust tests, three Godot bridge suites, 48 paired fixtures, interaction and saved-city replays pass. |
| `ROAD-19` | `feature` | `done` | `P2` | [`roads.md`](roads.md) | The settled road preview showed only the new stroke, hiding the junction, curb and marking changes that commit would create. | Reuse the exact worker's compiled local scene and committed renderer; replace cached source owners reversibly without changing simulation state. Five cold-commit geometry comparisons, all 1,562 Rust tests and four Godot suites pass, including water-only query changes. Three matched process pairs measure about 67 ms T preview readiness versus 61–67 ms for the ribbon; no speedup claim. Future terrain excavation remains outside the preview contract. |
| `ROAD-20` | `hardening` | `done` | `P2` | [`roads.md`](roads.md) | Junction-preview validation found side-32 T commits regressing 182→207 ms and a smaller 112-edge first-stroke regression. Follow-up profiling identifies about 32 ms rebuilding a redundant whole-network ghost-snap index on each edit; it does not establish mesh-owner bookkeeping as the cause of the historical delta. | Reuse the existing edge R-tree with allocation-free local guide queries. Three matched release/headless pairs reduce side-32 T click-to-first-idle 199→141 ms (22–33% per pair), snapshot 40.6→6.1 ms, and warmed 112-edge first road 54.1→39.5 ms. Guide/graph/lane output matches; 1,575 Rust tests, five Godot suites and single-worker replay pass. Dense cursor queries cost about 0.10 ms; empty-network results are inconclusive and preview readiness is not uniformly faster. |
| `ROAD-21` | `feature` | `done` | `P2` | [`roads.md`](roads.md) | Idle-gated junction previews showed no junction updates during continuous mouse movement. | Replace the idle scheduler with immediate, bounded latest-input work; retain the last completed display separately from exact click validation and reuse unchanged retained CPU/GPU meshes. Three headless captures show 40–72 updates per 96 moving inputs, median new-mesh latency 19–37 ms, with no post-startup disappearance. All 1,566 Rust tests, five Godot suites, the 48-fixture matrix and interaction/scaling/saved-city/single-worker replays pass. Commit recovery is measured separately in the ROAD-20 follow-up. |
| `ROAD-22` | `bug` | `done` | `P2` | [`roads.md`](roads.md) | Compiled previews were gated on JunctionN, excluding bends/straight continuations; hover validation also built unused ribbons, and pending checks could waste completed poses. | Export all affected connected surface kinds, match commit's terminal-reprofile dirty ledger, build fallback geometry only on demand and retain completed poses through pending-check recovery. Cold-commit geometry comparisons, 1,569 Rust tests, five Godot suites and layout/rendered regressions pass. Isolated hover-validation p50 falls 78→40 µs (two lanes), 135→56 µs (eight); moving bends show 66–90 updates/96 inputs at 18–19 ms median new-mesh latency. No stable overall junction speedup or ROAD-20 recovery claimed. |
| `ROAD-23` | `bug` | `done` | `P1` | [`roads.md`](roads.md) | Compiled previews lifted existing road meshes away from committed terrain cutouts; bends also exposed the vacated terminal footprint. Solid-plane capture fixtures hid the defect. | Pin existing coverage, including inserted contact vertices; preserve seam breakpoints and add reversible ground for vacated footprints. Nine real-terrain before/preview/cancel/commit fixtures reduce preview-only sky pixels from 142–355 to zero. All 1,573 Rust tests, five Godot suites and single-worker replay pass. Three matched motion pairs retain roughly 19–37 ms median latency; wide-bend updates fall in two pairs. This corrects rendering without terrain mutation, not a performance win. |
| `ROAD-24` | `bug` | `done` | `P0` | [`roads.md`](roads.md#kuopio-terrain-regression-replay-road-24) / [`terrain.md`](terrain.md) | RoadEditPlan owns the final local topology, profiles, sites, structural samples and road/terrain products, with exact readiness dependencies, atomic adoption and rollback/undo. Ownership is separate from elevation, with the 32 m transition retained. Shared halo grading, one guide-height authority, strict tile bounds and metric constraint incidence fix the captured blue gaps; discarded terrain faces and missing/invalid final buffers block paired publication; pre-composition checks remain separate from publication validation. Obsolete regrade APIs/telemetry are removed. The profile finalizer's full dirty ledger is compiled before preview. Load repairs rejected grounded node 34 through that same finalizer, preserving the reference SQLite and nonincident saved profiles. All 39 fixed-location strokes, 158 profiles and 27 saved terrain patches pass; windowed and final optimized headless replays report zero audit failures. All 1,634 Rust tests, 15 Python checks, five Godot suites and single-worker regressions pass. | Controlled performance verification is complete with an explicit small-cost tradeoff, not a speedup claim. Three 20-repetition matched pairs measure second-T readiness 50.89→53.98 ms and mixed-width crossing 67.37→69.70 ms; first-idle is effectively unchanged. Three 100-observation populated-worker runs retain identical local products at 100,000 background buildings / 600,024 agents, with worker p50 20.19→20.52 ms and readiness below 0.019 ms. Work stays local; full-FSM city FPS and universal arbitrary-input watertightness are not claimed. |
| `ROAD-25` | `bug` | `done` | `P0` | [`roads.md`](roads.md#third-road-boundary-and-placement-feedback-road-25) | Third-road terrain compilation lost short owned node-boundary segments; empty rejected preview payloads erased feedback, and queued clicks were presented as committed. | Preserve every distinct canonical boundary edge, retain visible rejection feedback, and acknowledge simulation acceptance before finishing the gesture. All 1,767 active Rust tests, three Godot road suites and 144 reconstructed three-road layouts pass. Three matched release pairs retain identical local products through 100,000 background buildings / 600,024 agents; worker p50 is 20.658 → 20.862 ms at the largest size. |
| `BUILD-01` | `bug` | `done` | `P1` | [`building_allocator.md`](building_allocator.md) | Load reconstructed explicit buildings from `cell_x = 0` instead of saved `frontage_t`, relocating the iso farms by 194–260 m while keeping their original support heights. | Placement and load share fractional-frontage transforms; regressions preserve centers, facing, setback, site footprints, and support heights on both road sides, graded bends, and through SQLite. |
| `TRAFFIC-01` | `bug` | `done` | `P1` | [`traffic.md`](traffic.md) / [`entrance_and_exit.md`](entrance_and_exit.md) | Loading discarded sidewalk lanes, interpreted every saved junction connector as vehicle lane 0, and retained stale planned lane IDs after graph/lane rebuilds. | Version 58 snapshots identify directed road lanes and connector endpoints, remap current/attach/detach references, and restore lane heights before agents. Version 57 recovery uses saved route/pose and building-side topology. The iso replay restores all 14 junction occupants and both stranded final-leg pedestrians without missing connectors or pedestrians entering vehicle lanes. |
| `SIM-01` | `bug` | `done` | `P0` | [`terrain.md`](terrain.md) | Terrain/water Rayon jobs blocked on `SimCore` while the simulation held that mutex and awaited parallel agent work. The 2026-09-06 hang stopped at `agent tick`; a saved-city streaming replay reproduced it without service placement. | Worker snapshots use nonblocking acquisition and the existing request retry protocol; completed refined payloads wait for generation-checked cache insertion during nonblocking polling. Contention/cache-retention regressions, all 1,532 Rust tests, both Godot bridges, the road/save replay, and single-worker/default-pool streaming with 24 power-plant previews per run pass. |
| `SAVE-01` | `bug` | `done` | `P1` | [`ui.md`](ui.md) | City snapshots omitted the camera, so loading retained the current view or the new scene's default framing. | Version 59 stores the active camera's pivot, yaw, pitch, orbit distance, and projection with the world. Native load restores controls before renderer refresh; versions 57/58 remain loadable. SQLite round-trip/legacy checks and headless existing/fresh-camera, projection, continued-input, and camera-less-save regressions pass. |

### `EARTH-01`

- **Observed in debug logs**: elevated four-way road tests can report clean terminal topology, clean mouth seams, and `cdt_invalid_constraints=0`, while still showing high terrain seam steepness such as `cdt_road_seam_max_slope > 1.0`.
- **Scope**: this is not a curb / sidewalk ownership failure. The road-owned top surface and terminal sideskirts can be correct while the surrounding terrain tie-in is visually too abrupt.
- **Current state**: road-touched terrain CDT keeps grounded `Standard` seam faces on the ordinary terrain path by inserting explicit grade-limited guide samples around the final road-owned footprint, omitting source samples that would force an over-steep cut / fill tie-in, and exporting those widened source / seam sample decisions in terrain debug output. Ordinary standard/node seam sources do not emit retaining-wall topology; retaining-wall output remains source-gated for structural / future retaining cases. Synthetic DEM coverage still validates structural retaining-wall classification, while production authored plus compact imported Kuopio DEM coverage validates ordinary lower-shelf tie-ins, steep grounded terrain, raised standard spans, raised terminals / bends near ridge-valley terrain, raised multiway junctions, and edit-order-stable emitted terrain-CDT topology.
- **Closure**: the first imported DEM failure was an internal JunctionN road-owned chord reaching the terrain seam constraint set. The fix keeps final road-owned footprints authoritative, drops only constraints whose exact side probes both classify as road-owned, and preserves exposed seam constraints with source provenance. Future closure variants beyond the structural retaining-wall path should get a new `EARTH-*` item instead of reopening `ROAD-01`.

### `EARTH-02` road/site terrain follow-up

- The changeset audit corrects opposite-sign runtime mesh-part yaw and shares transforms between
  rendering, site support and frontage attachment. Duplicate selection/lifecycle/bounds paths and
  unused centre-normal queries are removed. Revision 13 and verification are recorded in
  [`earthworks.md`](earthworks.md#changeset-audit-2026-09-11); broader frame-publication work remains open.
- The neighboring commercial-yard slit is fixed by welding near-side grading samples before
  tile containment. Both tiles now retain the same point/height; the loaded scene's 27.7 cm
  T-junction disagreement is gone, without moving its flat pad or modifying assets. Revision 12
  and paired-side regression coverage are documented in
  [`terrain.md`](terrain.md#shared-side-yard-seam-verification-2026-09-11).
- The remaining small-shop frontage wedge is fixed by replacing estimated mesh-support extents
  with cached imported model bounds. A 10×10 m commercial regression covers both road sides and
  slope directions; the reported scene passes 99 seam probes and eight terrain patches without
  modifying assets or the save. Verification and startup/query cost are recorded in
  [`earthworks.md`](earthworks.md#imported-structural-bounds-verification-2026-09-11).
- The 2026-09-11 missing-patch defect is fixed in shared CDT noding: distinct intersections near
  pad corners survive cleanup, and invalid terrain still cannot publish. The synthetic regression
  covers constraint preservation, exterior coverage and deterministic ordering; current correctness
  and compiler-cost measurements are recorded in [`terrain.md`](terrain.md#roadsite-corner-noding-verification-2026-09-11).
- This closes that specific terrain-publication defect, not the broader `EARTH-02` work item.

### `EARTH-03`

- **Observed in debug logs**: arbitrary-angle junction mouths could report invalid terrain-CDT helper constraints and still show steep mouth facets after the ordinary grade-limited tie-in pass.
- **Scope**: this is not a render material, texture, contour, or retaining-wall issue. The roadbed footprint is still authoritative, but the near-road grading envelope must be generated from that final footprint and must not add constraints that can cross concave road-owned regions.
- **Closure**: grading-envelope generation now lives with `RoadSurfaceSystem` terrain-CDT export. It consumes the final unioned roadbed loops, emits deterministic grade-limited guide samples before ordinary source samples, constrains guide rails only for a single clean non-hole convex loop, leaves hole / concave / multi-loop footprint sets sample-only, excludes building-site loops from roadbed grading, and preserves exposed road seam constraints.

### `NET-01`

- **Root cause**: `collect_crossing_splits` in `rust/src/simulation/network/topology.rs` skips any segment pair where `v1.dot(v2).abs() > 0.98`. This guard was intended to avoid false intersections on near-parallel roads, but it also fires for two distinct edges that are drawn in the same direction — including when the player draws a road overlapping or branching off an existing road. No junction node is created, and the two edge objects remain topologically disconnected.
- **Downstream effect**: `freight_car_eta_between_buildings` calls `cch_graph.find_path` between the two disconnected segments and gets `None`. `try_local_supplier_for_resource` returns `false` for every local candidate. All freight falls back to OWA permanently — local supply chains never form.
- **Observed in gameplay**: grocery imported 474 packaged_food from OWA at 1.75× local price on first order despite operational local food producers; producers earned only OWA export revenue, insufficient to cover wages, trending toward bankruptcy by day 26.
- **Fix**: removed the `v1.dot(v2).abs() > 0.98` guard entirely. Truly parallel edges return `None` from `find_intersection_2d` (denom ≈ 0) so the guard was redundant for the intended case and harmful for shallow-angle distinct-edge crossings. See `topology.rs:459`.

## Large-World Track

| ID | Kind | Status | Priority | Owner doc | Problem | Exit criteria |
|----|------|--------|----------|-----------|---------|---------------|
| `WORLD-01` | `feature` | `open` | `P2` | [`project.md`](project.md) | Regions outside the active area of interest still lack aggregate simulation. | Inactive regions run at aggregate fidelity with documented behavior and integration points. |
| `WORLD-02` | `feature` | `open` | `P2` | [`project.md`](project.md) | Promotion and demotion rules between full-FSM, flow-field, and aggregate tiers are not defined in live code. | Tier transitions are implemented and documented with deterministic promotion/demotion rules. |
| `WORLD-03` | `feature` | `open` | `P2` | [`project.md`](project.md) | There is no world overview for inspecting inactive-region aggregate state. | The world overview can inspect aggregate inactive-region state and flows at a useful gameplay level. |

## Transport Expansion Track

| ID           | Kind      | Status | Priority | Owner doc                  | Problem                                                                                                                                    | Exit criteria                                                                                                                                                                         |
| --------------| -----------| --------| ----------| ----------------------------| --------------------------------------------------------------------------------------------------------------------------------------------| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `TRANSIT-01` | `feature` | `open` | `P2`     | [`project.md`](project.md) | Bus support is not yet layered onto the shared vehicle and waiting-state foundation.                                                       | Bus routing, waiting, and vehicle flow run on the shared transport foundation in live gameplay.                                                                                       |
| `TRANSIT-02` | `feature` | `open` | `P2`     | [`project.md`](project.md) | Rail and metro support are still missing from isolated `RAIL` routing.                                                                     | Rail routing and the first rail gameplay loop are implemented on isolated `RAIL` paths.                                                                                               |
| `TRANSIT-03` | `feature` | `open` | `P2`     | [`project.md`](project.md) | Harbor-linked ship and ferry transport does not exist yet.                                                                                 | Water transport is supported through harbor-linked water routes with live routing behavior.                                                                                           |
| `TRANSIT-04` | `feature` | `open` | `P2`     | [`project.md`](project.md) | Air and border travel support are still absent.                                                                                            | Airport and border-node travel paths are implemented with live routing support.                                                                                                       |
| `TRANSIT-05` | `feature` | `open` | `P1`     | [`project.md`](project.md) | Bicycle support is still the next missing real transport mode on top of the multi-modal foundation.                                        | Bicycles are supported as a live transport mode with routing, movement, and gameplay-facing behavior.                                                                                 |
| `TRANSIT-06` | `feature` | `open` | `P1`     | [`project.md`](project.md) | Pedestrian and bicycle movement still lacks bounded local steering, and future high-density station crowds need a dedicated waiting model. | Active pedestrians and bicycles use deterministic corridor-local steering, while dense passenger waiting uses a separate slot/queue model instead of naive all-to-all crowd steering. |

- Steering reference for `TRANSIT-06`: Craig W. Reynolds, [*Steering Behaviors For Autonomous Characters*](https://www.red3d.com/cwr/steer/gdc99/). Treat it as a local-movement reference rather than a replacement for exact route planning or dense waiting-state simulation.

## Tools And Content

| ID | Kind | Status | Priority | Owner doc | Problem | Exit criteria |
|----|------|--------|----------|-----------|---------|---------------|
| `TOOLS-01` | `feature` | `done` | `P2` | [`ui.md`](ui.md) / [`asset_editor.md`](asset_editor.md) | Installed content packs can now be viewed and enabled through the shared Options window's `Mods` category from both MainMenu and gameplay. | Players can view and manage installed content packs through an in-game options UI. |
| `TOOLS-02` | `feature` | `done` | `P1` | [`ui.md`](ui.md) / [`roads.md`](roads.md) / [`building_allocator.md`](building_allocator.md) | Gameplay bulldoze now exists as its own bottom-right tool rather than a special case inside selection. | One click queues deletion of exactly one Rust-targeted building or road, structural/render rebuilds stay off the Godot thread, road terrain/render state is repaired, and building deletion is undo-safe across allocator/economy references. |
| `TOOLS-03` | `feature` | `open` | `P2` | [`asset_editor.md`](asset_editor.md) | The whole building stock is the Kenney city kit, which is a stylised international kit and reads as cartoon against the Finnish landscape the vegetation now targets. Finnish reference photographs taken 2026-09-13 show what the kit cannot express: `rintamamiestalo` (the post-war one-and-a-half storey gable house on a square plan), flat-roofed and shallow-pitched `1960-80s` detached houses, standing-seam or profiled sheet metal roofs in dark grey and red, painted board or light render walls, and a clipped hedge rather than asphalt between the house and the street. Measurement rules scale out as the cause: `building-type-a.glb` is `1.3 x 0.834 x 1.028` units at `scale = 7`, so `9.1 m x 5.8 m x 7.2 m`, which is a plausible one-storey house, and the median tree beside it is about `19 m`, a ratio the reference photographs agree with. The mismatch is style and site treatment, not size. The asset manifest format already carries everything a replacement needs - mesh parts, LOD distances, entrance, driveway and parking anchors, and site surface polygons - so this is asset sourcing or generation plus manifests, not a code change. | A residential set that a Finnish viewer reads as a Finnish street exists as a content pack, covering at least a `rintamamiestalo` and one later detached type, with licensing recorded. Site surfaces put lawn and hedge between the house and the street rather than an asphalt apron. No renderer or registry code changes to support it. |

## Parked Historical Reports

These reports are not current blockers or pending RoadEditPlan acceptance work. `ROAD-12`
rollback and `ROAD-24` readiness/atomic adoption supersede their original commit-safety gap;
the historical geometry failures have not been verified against the current implementation.
Parking preserves the evidence without claiming a geometry fix. Detailed contracts remain in
[`roads.md`](roads.md) and [`terrain.md`](terrain.md).

| ID | Kind | Status | Historical evidence | Reopen condition |
|----|------|--------|---------------------|------------------|
| `ROAD-06` | `bug` | `parked` | Automated Kuopio T-junction benchmark, not the manual placement capture. The retained local `benchmark-results/gameplay-roads-windowed-20260903T202539Z.godot.log` records a commit-settlement timeout at `(-520, -1040)` with `520 m` spacing. The old report also named `(-1280, -1280)` and `(1280, -1280)` in the extended `640 m` layout. Benchmark artifacts are ignored, not repository fixtures. | A current-build replay reproduces a failure, with retained map identity, exact inputs, and diagnostic output. Reassess severity from that evidence. |
| `ROAD-07` | `bug` | `parked` | The old report named a curved Kuopio bend at `(640, 1280)`, patch `(18, 20)`, and `missing_road_boundary_source`. Its exact failure capture was not located in the retained logs during the 2026-09-09 review. | A current-build replay reproduces the provenance failure, with retained map identity, exact curve inputs, and diagnostic output. Reassess severity from that evidence. |

## Notes

- The old numbered backlog and bug table are archived in [`archive/project_legacy_2026-04-09.md`](archive/project_legacy_2026-04-09.md).
- New work should never introduce fresh `item N` references. Use stable IDs instead.
- Keep roadmap rows short: describe the current problem and a visible exit condition, then put subsystem detail in the owner doc.
- The Criterion microbenchmark suite was expanded to cover `ACCESS_EGRESS` and `ACCESS_INGRESS`. Benchmark deltas against older saved results may therefore reflect a changed suite shape, not only a runtime regression.
